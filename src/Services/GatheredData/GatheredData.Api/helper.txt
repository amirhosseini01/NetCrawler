# switch to mongo command in docker cli
mongosh

# open or create db
use <your db name>

# create collection
db.createCollection('your collection name')

# open docker container in terminal
docker exec -it <container name> /bin/bash
